# Legacy pinned requirements for an older Python 3.10 HST-only stack.
# Prefer environment.yaml + `pip install -e .` (see README.md / pyproject.toml).
#
# These pins fail on Python 3.12 (astropy 5.3.3 builds from source against
# modern setuptools). Kept only for historical reference.

astropy==5.3.3
astroquery==0.4.6
astroscrappy==1.1.0
drizzlepac==3.5.1
numpy==1.24.3
progressbar33==2.4
python_dateutil==2.8.2
Requests==2.32.0
scipy==1.10.1
stwcs==1.7.2
